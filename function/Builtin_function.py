'''Built-in functions are functions that are already "pre-installed" in Python. we don't need to define  or
import any external libraries to use them
eg. 
1.input/output function
 print():ie display the data on screen
 input():ie takes data from the user
2.Mathematical function
 a.abs():Return the positive value number
 number=-5
  result=abs(number)
  print(result)
  b.pow(x,y) :return x to the power y
  c.max()/min():find the largest and smallest number from list
     a=[1,2,3]
      largest=max(a)
     print(largest)
d.len(),dir(), strip(), range(),etc
'''

